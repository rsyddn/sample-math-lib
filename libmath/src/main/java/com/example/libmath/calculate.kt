package com.example.libmath

class calculate {
    fun time(first: Int, second: Int): Int = first * second
    fun add(first: Int, second: Int): Int = first + second
    fun subtract(first: Int, second: Int): Int = first - second
    fun devide(first: Int, second: Int): Int = first / second
}